{{ 'section-custom-sonang-section.css' | asset_url | stylesheet_tag }}

<div class="sonang">
  {%- for block in section.blocks -%}
    {%- case block.type -%}
      {%- when 'title' -%}
        <h2 class="banner__title inline-richtext">
          {{ block.settings.title }}
        </h2>
      {%- when 'description' -%}
        <div class="banner__description">
          <p>{{ block.settings.description }}</p>
        </div>
    {%- endcase -%}
  {%- endfor -%}

  <div class="sonang__img">
    {{
      section.settings.hero | image_url: width: 300 | image_tag
    }}
  </div>
</div>


{% schema %}
  {
    "name": "Custom Sonang Section",
    "tag": "section",
    "class": "sonang",
    "settings": [
      {
        "type": "image_picker",
        "label": "hero",
        "id": "hero"
      }
    ],
    "blocks": [
      {
        "type": "title",
        "name": "title",
        "settings": [
          {
            "type": "text",
            "label": "Title",
            "id": "title"
          }
        ]
      }, {
        "type": "description",
        "name": "description",
        "settings": [
          {
            "type": "text",
            "label": "Description",
            "id": "description"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Custom Sonang Section",
        "blocks": [
          {
            "type": "title"
          }, {
            "type": "description"
          }
        ]
      }
    ]
  }
{% endschema %}